---
description: Record a standing preference about how you want to be taught
argument-hint: what you want more or less of
---

The learner is telling you how they want to be taught. Treat this as durable
state, not a remark about the current question.

Their words: **$ARGUMENTS**

Do three things, in order.

**1. Read `/vault/objectives.md`.** It is the standing calibration and you
should already have read it this session. If it does not exist, create it from
the shape below.

**2. Record this.** Edit the file so the preference survives the session.
Put the countable part in front matter and the judgment in prose — the same
split as a curriculum topic's `relevance`, and for the same reason: the enum
makes it checkable, the sentence makes it true.

```yaml
---
emphasis: [...]      # what to spend questions on
deemphasis: [...]    # what not to test, even when the source covers it
register: ""         # how to pitch it
---
```

Add a dated line under `## Log` saying what changed and, where they gave one,
why. A preference with its reason attached survives being re-read in three
months; a bare rule reads as arbitrary and gets ignored or over-applied.

**Do not restate what is already there.** If this sharpens an existing entry,
edit that entry. If it contradicts one, say so out loud and ask which wins
rather than silently keeping the newer.

**3. Apply it now.** Say in one line what you will do differently for the rest
of this session. If a question you already posed violates it, say that too —
the calibration is retroactive as far as the current topic.

Then continue where you left off. This is a correction, not a new session.
