# Notes

One directory per subject. Suggested starting points, based on what you said
you're after:

```
notes/linear-algebra/
notes/statistics/
notes/os-internals/
notes/information-theory/
```

Nothing enforces this. Obsidian doesn't care about directory structure and
neither does the teach skill — links matter more than folders.

## Open question

Whether this stays one vault or splits per subject is `docs/OPEN.md`
decision 2. The argument for one: the crossover case (launching with
`--subject ~/code/meta` and having design context land in the same graph)
only works if there is one graph.
